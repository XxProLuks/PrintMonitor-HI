# ============================================================================
# CONFIGURAÇÕES DO SERVIDOR PRINT MONITOR
# ============================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Execute: python gerar_secret_key.py para gerar uma SECRET_KEY
# 3. Cole a chave gerada abaixo
# 4. Ajuste as outras configurações conforme necessário
#
# IMPORTANTE: NUNCA commite o arquivo .env no Git!
# ============================================================================

# SECRET_KEY - OBRIGATÓRIA EM PRODUÇÃO
# Gere uma chave segura executando: python gerar_secret_key.py
SECRET_KEY=sua-chave-secreta-aqui-gerar-com-gerar_secret_key.py

# Ambiente (development ou production)
FLASK_ENV=development

# Nome do banco de dados
DB_NAME=print_events.db

# Configurações de sessão
SESSION_COOKIE_SECURE=False  # True em produção com HTTPS
SESSION_LIFETIME=3600  # Tempo de vida da sessão em segundos (1 hora)

# Connection Pool (opcional)
DB_POOL_MAX_CONNECTIONS=10
DB_POOL_TIMEOUT=5.0
DB_POOL_MAX_RETRIES=3
DB_POOL_RETRY_DELAY=0.5

# Rate Limiting (opcional)
RATELIMIT_STORAGE_URL=memory://

# ============================================================================
# CONFIGURAÇÕES DO AGENTE (se aplicável)
# ============================================================================
# SERVER_URL=http://localhost:5000
# LOG_LEVEL=INFO

